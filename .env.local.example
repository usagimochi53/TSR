# Google Places API integration preview.
# v1.6 does not call the real API yet. v1.7 will use these values.
NEXT_PUBLIC_ENABLE_PLACES_API=false
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# NEXT_PUBLIC_* values are exposed to the browser.
# Restrict this key in Google Cloud Console with HTTP referrers and API restrictions.
